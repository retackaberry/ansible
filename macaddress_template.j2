{% for item in ansible_play_hosts %}
{% set short_name = item.split('.') %}
dhcp-host={{ hostvars[item].ansible_default_ipv4.macaddress|upper+','+hostvars[item].ansible_default_ipv4.address+','+short_name[0] }}
{% endfor %}
